# Cloudflare Worker — Gallery API (lists R2 images for portfolio)
# Deploy: npx wrangler deploy
# Set YOUR_BUCKET_NAME to your R2 bucket that serves https://chandralekha.me/images/

name = "gallery-api"
main = "index.js"
compatibility_date = "2024-01-01"

[[r2_buckets]]
binding = "IMAGES"
bucket_name = "YOUR_BUCKET_NAME"
